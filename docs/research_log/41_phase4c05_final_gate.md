# Phase 4C-0.5 Final Gate

## A. Repository

Checkpoint `336ba7666540fef0d4660167218d4c34e5f8311c` was verified before this audit on
branch `research/phase4c0-scientific-audit`. The audit changes only offline tools,
derived evidence and protocol documents. No formal Phase 4C run was started.

## B. Attribution Target Neutrality

**PASS after replacing the Phase 4C-0 biased definition.** H* is the selected
high-level loop vertex, original baseline historical anchor, and exactly seven
chronological pre-loop keyscans. The operator and cardinality are identical across
A/B/C and do not read `oracle_mode`.

## C. Phase4B Neutral Reanalysis

- A: 0/21 target-attributable/executed;
- B: 0/21;
- C: 1/21.

## D. Local ROI Neutrality

**PASS.** ROI centre is the method-neutral prior-graph loop vertex and radius is
5.0 m; A/B/C ROI sequences are identical for all five Phase 4B seeds.

## E. map4 Opportunity Adequacy

Per-seed planned loops: `1,1,1,1,1`; total 5. **FAIL.**

## F. map7 Opportunity Adequacy

Per-seed planned loops: `1,1,1,1,1`; total 5. **FAIL.**

## G. Selection-Sequence Protocol

Every future run must store `selected_loop_vertex_sequence`, `selection_order`,
`selection_timestamp`, `initial_tsp_hash`, and `full_tsp_hash`. “Loop targets
matched” is allowed only when sequences are identical. Otherwise only the
algorithm/objective is held fixed; common-target analysis is secondary and the
primary unit remains map-seed.

## H. Remaining Nonblocking Limitations

Closure-specific graph gain is unavailable; only Karto is tested; target events are
rare; Stage is stochastic; the 4 m proxy is uncalibrated; and the nominal 12 m
repair extension can overshoot on its terminal segment. These remain nonblocking,
but map opportunity inadequacy is blocking.

## I. Decision

**PROTOCOL_BLOCKED**

map8 passes as one blind replacement candidate, but no second unused author
map/prior pair meets the frozen opportunity gate. The 30-run map4/map7 matrix remains
`NOT_STARTED` and is not authorized.

# STOP
